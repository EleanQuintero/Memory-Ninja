// Divide por salto de línea, punto y coma, coma, o DOS O MÁS espacios/tabulaciones
export const PROCESS_PATERN = /[\n;,]+|[ \t]{2,}/

// Mensajes de request
export const REQUEST_MESSAGES = {
    UNAUTHORIZED_MESSAGE: 'Usuario no encontrado, se necesita autenticacion' }
    